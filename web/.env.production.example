# Production Environment Configuration
# Copy this file to .env.production and fill in your values
# DO NOT commit .env.production to version control!

# Supabase Configuration (from Supabase Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Python API Configuration (from terraform output)
# Run: cd infrastructure/terraform && terraform output lambda_url
NEXT_PUBLIC_API_URL=https://your-lambda-url.lambda-url.us-east-1.on.aws

# Strava Webhook Configuration
# Generate with: openssl rand -base64 32
STRAVA_WEBHOOK_VERIFY_TOKEN=your-random-secure-token-here

# Environment
NEXT_PUBLIC_ENV=production

# Instructions:
# 1. Deploy infrastructure: cd infrastructure/terraform && terraform apply
# 2. Copy Lambda URL from terraform output to NEXT_PUBLIC_API_URL above
# 3. Build the web app: ./scripts/build-web.sh
# 4. Deploy to S3: aws s3 sync web/out/ s3://<bucket-name> --delete
# 5. Invalidate CloudFront: aws cloudfront create-invalidation --distribution-id <dist-id> --paths "/*"
