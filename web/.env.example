# Supabase Configuration
# Get these values from: https://supabase.com/dashboard/project/<your-project-ref>/settings/api

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Strava Webhook Configuration
# This token is used to verify webhook requests from Strava
# Generate a random secure string (e.g., using: openssl rand -base64 32)
# REQUIRED for webhook functionality
STRAVA_WEBHOOK_VERIFY_TOKEN=your-random-secure-token-here

# TrainingPeaks OAuth Configuration (Partner API)
# Apply for API access at: https://api.trainingpeaks.com/request-access
# Response time: 7-10 business days
TRAININGPEAKS_CLIENT_ID=your-client-id-here
TRAININGPEAKS_CLIENT_SECRET=your-client-secret-here
TRAININGPEAKS_ENV=sandbox  # or 'production'

# Python API Configuration
# URL of the FastAPI backend for compliance analysis
# Development: http://localhost:8000
# Production: https://your-api-domain.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual credentials
# 3. For STRAVA_WEBHOOK_VERIFY_TOKEN, generate a random secure string
# 4. For TrainingPeaks, apply for API access and use sandbox for testing
# 5. For NEXT_PUBLIC_API_URL, use http://localhost:8000 in development
# 6. Never commit .env.local to git (it's already in .gitignore)
